import { EmptyScreen } from "@/components/EmptyScreen";

export default function HistoryPage() {
  return (
    <EmptyScreen
      title="History"
      description="Training heatmap and session log will appear here after logging is built."
    />
  );
}
