import { EmptyScreen } from "@/components/EmptyScreen";

export default function ProgressPage() {
  return (
    <EmptyScreen
      title="Progress"
      description="PR wall, volume charts, and plateau flags will land here in a later phase."
    />
  );
}
